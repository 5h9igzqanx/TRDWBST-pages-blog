---
title : '三角函数、三角形相关公式'
date : 2026-01-04T19:32:43+08:00
tags : ['学习资料']
categories : ['笔记']
author : '5h9igzqanx'
description : '鉴于我对于三角函数的相关公式的掌握程度不熟练，现将课本上的笔记摘抄于此。原文可看《普通高中就教科书·数学 （A版） 必修 第一册》（人民教育出版社）的第五章'
draft : false
---

# 三角函数、三角形相关公式

## 平方和

令

$$
\alpha\in\mathbb{R}
$$

有

$$
\sin^2\alpha+\cos^2\alpha=1
$$

## 正切定义

令

$$
\alpha\in\mathbb{R}
$$

有

$$
\tan\alpha=\frac{\sin\alpha}{\cos\alpha}
$$

## 诱导公式

令

$$
\alpha\in\mathbb{R}\\ k\in\mathbb{Z}
$$

有

### 一

$$
\sin(\alpha+k\cdot2\pi)=\sin\alpha\\\cos(\alpha+k\cdot2\pi)=\cos\alpha\\\tan(\alpha+k\cdot2\pi)=\tan\alpha
$$

### 二

$$
\begin{align}\sin(\pi+\alpha)&=-\sin\alpha\\\cos(\pi+\alpha)&=-\cos\alpha\\\tan(\pi+\alpha)&=\tan\alpha\end{align}
$$

### 三

$$
\begin{align}\sin(-\alpha)&=-\sin\alpha\\\cos(-\alpha)&=\cos\alpha\\\tan(-\alpha)&=-\tan\alpha\end{align}
$$

### 四

$$
\begin{align}\sin(\pi-\alpha)&=\sin\alpha\\\cos(\pi-\alpha)&=-\cos\alpha\\\tan(\pi-\alpha)&=-\tan\alpha\end{align}
$$

### 五

$$
\sin(\frac{\pi}{2}-\alpha)=\cos\alpha\\\cos(\frac{\pi}{2}-\alpha)=\sin\alpha
$$

### 六

$$
\begin{align}\sin(\frac{\pi}{2}+\alpha)&=\cos\alpha\\\cos(\frac{\pi}{2}+\alpha)&=-\sin\alpha\end{align}
$$

## 和差角

令

$$
\alpha\in\mathbb{R}\\ \beta\in\mathbb{R}
$$

有

### 差角

$$
\begin{align}\sin(\alpha-\beta)&=\sin\alpha\cos\beta-\cos\alpha\sin\beta\\\cos(\alpha-\beta)&=\cos\alpha\cos\beta+\sin\alpha\sin\beta\\\tan(\alpha-\beta)&=\frac{\tan\alpha-\tan\beta}{1+\tan\alpha\tan\beta}\end{align}
$$

### 和角

$$
\begin{align}\cos(\alpha+\beta)&=\cos\alpha\cos\beta-\sin\alpha\sin\beta\\\sin(\alpha+\beta)&=\sin\alpha\cos\beta+\cos\alpha\sin\beta\\\tan(\alpha+\beta)&=\frac{\tan\alpha+\tan\beta}{1-\tan\alpha\tan\beta}\end{align}
$$

## 二倍角

令

$$
\alpha\in\mathbb{R}
$$

有

$$
\begin{align}\sin2\alpha&=2\sin\alpha\cos\alpha\\\cos2\alpha&=\cos^2\alpha-\sin^2\alpha\\\tan2\alpha&=\frac{2\tan\alpha}{1-\tan^2\alpha}\end{align}
$$

其中

$$
\begin{align}
\cos2\alpha&=1-2\sin^2\alpha\\
&=2\cos^2\alpha-1
\end{align}
$$

## 半角

这里的“半角”公式是由上面的“二倍角”公式化得的。

令

$$
\alpha\in\mathbb{R}
$$

有

### “小万能”

$$
\sin^2\frac{\alpha}{2}=\frac{1-\cos\alpha}{2}\\
\cos^2\frac{\alpha}{2}=\frac{1+\cos\alpha}{2}\\
\tan^2\frac{\alpha}{2}=\frac{1-\cos\alpha}{1+\cos\alpha}
$$

### “大万能”

$$
\sin\frac{\alpha}{2}=\pm\sqrt{\frac{1-\cos\alpha}{2}}\\
\cos\frac{\alpha}{2}=\pm\sqrt{\frac{1+\cos\alpha}{2}}\\
\tan\frac{\alpha}{2}=\pm\sqrt{\frac{1-\cos\alpha}{1+\cos\alpha}}
$$

其中正负号由$\frac{\alpha}{2}$所在象限决定。

## 积化和差、和差化积

令

$$
\alpha\in\mathbb{R}\\
\beta\in\mathbb{R}
$$

有

### 积化和差

$$
\begin{align}
\sin\alpha\cos\beta&=\frac{1}{2}[\sin(\alpha+\beta)+\sin(\alpha-\beta)]\\
\cos\alpha\sin\beta&=\frac{1}{2}[\sin(\alpha+\beta)-\sin(\alpha-\beta)]\\
\cos\alpha\cos\beta&=\frac{1}{2}[\cos(\alpha+\beta)+\cos(\alpha-\beta)]\\
\sin\alpha\sin\beta&=-\frac{1}{2}[\cos(\alpha+\beta)-\cos(\alpha-\beta)]
\end{align}
$$
