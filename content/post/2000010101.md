---
title : '测试用文档'
date : 2000-01-01T08:00:00+08:00
tags : ['笔记']
categories : ['笔记']
author : "5h9igzqanx"
description : '这是用来测试此站点Markdown样式的文档，开发者可以通过这个文档对Markdown样式进行修改比对'
draft : false
---

# 测试用文档

## 标题测试

以下是标题测试

### 标题3

#### 标题4

##### 标题5

###### 标题6

## 文本标记测试

以下是文本标记测试（中文）：

正常文本用来显示正常内容；**粗体文本**用来显示着重；*斜体文本*用来显示引用、强调或者专有名词； ***加粗的斜体文本*** 则同时包括粗体文本和斜体文本的作用。<u>下划线</u>用来将短意群连接为长意群（Markdown原生语法不支持，或许使用HTML标签可以做到）；~~删除线~~用来标注审阅批注中删掉的部分。现在你已经大概了解了文本标记的用法，来试试吧。

The following is a text tagging test(English):

Normal text is used to display normal content;**Bold text** is used to display emphasis;*Italicized text* is used to display references, emphasis, or proper nouns;***Bold italic text*** includes both bold and italic text.<u>The underline</u> is used to connect short meaning groups into long meaning groups(Markdown native syntax is not supported, perhaps using HTML tags can achieve it); ~~The strikethrough~~ is used to mark the deleted parts in the review comments. Now that you have a general understanding of the usage of text tags, let's give it a try.

## 组件测试

### 引用组件测试

> 引用是指在说话或写作中引用现成的话，如诗句、格言、成语等，以表达自己思想感情的修辞方法。引用可分为明引、暗引两种。明引指直接引用原文，并加上引号，或者是只引用原文大意，不加引号，但是都注明原文的出处；暗引指不说明引文出处，而将其编织在自己的话语中，或是引用原句，或是只引大意。运用引用辞格，既可使文章言简意赅，有助于说理抒情；又可增加文采，增强表现力。——百度百科

> 如果你不知道一句名言是谁说的，那你就说是我说的。——马克·吐温

### 超链接组件测试

[百度一下，你就知道](https://www.baidu.com)

### 分隔符组件测试

---

### 图片组件测试

![图片](https://5h9igzqanx.github.io/TRDWBST-media/picture/blog/bgImg/home.png)

![图片2](https://5h9igzqanx.github.io/TRDWBST-media/picture/blog/bgImg/default.jpg)

## 列表测试

有序列表

1. 有序列表1
2. 有序列表2
3. 有序列表3

无序列表

* 无序列表1
* 无序列表2
* 无序列表3
  * 无序列表嵌套1
  * 无序列表嵌套2
    1. 无序列表嵌套有序列表1
    2. 无序列表嵌套有序列表2
  * 无序列表嵌套3

任务列表

* [x] 完成的任务
* [ ] 未完成的任务

## 表格测试

| 表头   | 居中对齐 | 右对齐  |
|:---- |:----:| ----:|
| 内容11 | 内容12 | 内容13 |
| 内容21 | 内容22 | 内容23 |

## 代码片段测试

测试语言分别是HTML、C、JavaScript和Python。

### HTML

```html
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <p>这是一个HTML文件。</p>
        <!-- 这是一条注释。 -->
    </body>
</html>
```

### C

```c
#include <stdio.h>
int main{
    printf("这是一个C程序。");
    return 0;
    // 这是一条注释。
}
```

### JavaScript

```javascript
function func(){
    var a = "这是一段JavaScript脚本。"
    document.InnerHTML = a
    console.log(a)
    // 这是一条注释。
}
```

### Python

```python
def func():
    a = "这是一个Python程序。"
    print(a)
    ## 这是一条注释。
func()
```

## 注释测试

在Markdown里支持插入注释，which可以使文章简洁一些。

这里有一条需要解释的内容[^1]，但是我不能使用小括号。这一条内容[^1]和上一条一样需要注释。

[^1]:[内容]：事物内部所含的实质或存在的情况。

注释里支不支持插入图片[^2]？

[^2]:[注释里……图片？]：这里也许可以有图片。  ![图片](https://5h9igzqanx.github.io/TRDWBST-media/picture/blog/bgImg/default.jpg)  在插入图片时需空两格。

## LaTeX数学公式测试（使用MathJax渲染）

平方差公式（1编号）

$$
\begin{equation} a^2-b^2=(a+b)(a-b) \tag{1} \end{equation}
$$

分式的性质（2编号，$\displaystyle m \neq 0$）

$$
\begin{equation} \frac{b}{a} = \frac{b * m}{a * m} \tag{2} \end{equation}
$$

质能公式（3编号）

$$
E=MC^2\tag{3}
$$

## 其他行内元素测试

这是<sup>上标</sup>和<sub>下标</sub>（通过原生HTML元素实现），这是<kbd>Esc</kbd> 退出键（通过原生HTML元素实现），这是`print()`print函数的实例，这是<ruby>拼<rt>pīn</rt>音<rt>yīn</rt>标<rt>biāo</rt>注<rt>zhù</rt></ruby>（通过原生HTML元素实现）

## 其他组件测试

### 告示框组件测试（通过Hugo短代码组件实现）

{{< ZX-noticeBox type=tip >}}
这是一条**提示**信息，用来提示**有更好的方法**。

例如你可以按<kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>Esc</kbd>来召唤任务管理器。
{{< /ZX-noticeBox >}}

{{< ZX-noticeBox type=warning >}}
这是一条**警告**消息，用来警告一些**注意事项**。

例如你在没有保存且打开了<kbd>FnLock</kbd>的情况下**千万不要**按<kbd>Alt</kbd>+<kbd>F4</kbd>！
{{< /ZX-noticeBox >}}

{{< ZX-noticeBox type=error >}}
这是一条**尤其注意**消息，用来说明一些**必须要注意的事项**。

例如你**千万不要**忘记你妈妈嘱咐过你**解冻冰箱里的肉**！
{{< /ZX-noticeBox >}}

### 行内图片组件测试（通过Hugo短代码组件实现）

这里有一段文字而这是一张图片：{{< ZX-inlineImg src=https://5h9igzqanx.github.io/TRDWBST-media/picture/blog/contentRes/2000010101-02.png height=5em alt=[点赞] >}}它被镶嵌在行内而这段话不止一行，那么这行字就会像这样。字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字字……

### 缩进组件测试（通过Hugo短代码组件实现）

例如，这里一段引用，但是不想用引用组件就可以这样：

{{< ZX-pIndent indentNum=2em >}}

这是空2格的缩进。

此致。

{{< /ZX-pIndent >}}

如是而已。

{{% ZX-pIndent indentNum=2em %}}

嗯。

{{% /ZX-pIndent %}}

### TOC（目录）测试

#### 假如是这是一个有很长标题的文档而这是它的标题

#### 假

#### 如

#### 说

#### 这

#### 是

#### 一

#### 个

#### 有

#### 很

#### 多

#### 标

#### 题

#### 的

#### 文

#### 档

#### 而

#### 这

#### 是

#### 它

#### 的

#### 标

#### 题
